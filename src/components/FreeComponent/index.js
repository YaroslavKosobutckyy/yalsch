import React from 'react';
import CardBithday from '../CardBithday';

const FreeComponent = (cardusers) => {

    return (
        <CardBithday cardusers={cardusers} />
    )
}

export default FreeComponent;